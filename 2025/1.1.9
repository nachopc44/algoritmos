//Una persona decide realizar un viaje a Europa, para lo cual necesita una determinada cantidad de euros. La persona tiene ahorrada una cierta suma en dólares y desea saber si es suficiente y, en caso de haber diferencia (de más o de menos) a cuantos pesos equivale. Realice un algoritmo que solucione el problema, para lo cual deberá prever que se ingresen las equivalencias de monedas que considere necesarias (por ejemplo la cotizacion en pesos de dólar y/o del euro, o a cuantos euros equivale un dólar).

Accion 119 es 
	AMBIENTE 
		ahorro, ahorroeuros, canteuros, precdolaraeuro, preceuroapeso, precdolarapeso: real 

	PROCESO
		Escribir("Ingrese la cantidad de dolares ahorrados")
		Leer(ahorro)
		Escribir("Ingrese la cantidad de euros que necesita")
		Leer(canteuros)
		Escribir("Ingrese el valor de un dolar en euros")
		Leer(precdolaraeuro)
		Escribir("Ingrese el valor de un dolar en pesos")
		Leer(precdolarapeso)
		Escribir("Ingrese el valor de un euro en pesos")
		Leer(preceuroapeso)

		ahorroeuros:= (ahorro * precdolaraeuro)
		Si(ahorroeuros > canteuros) entonces
			Escribir("Te sobran ", ((ahorroeuros - canteuros) * preceuroapeso), " pesos. O ", (ahorro - (canteuros / precdolaraeuro)), " dolares.")
		Sino 
			Si(ahorroeuros < canteuros) entonces
				Escribir("Te faltan ", ((canteuros - ahorroeuros) * preceuroapeso), " pesos. O ", ((canteuros / precdolaraeuro) - ahorro), " dolares.")
			Sino
				Escribir("Tenes la cantidad suficiente.")
			FinSi
		FinSi
	FinProceso
FINACCION