//Elaborar una función que reciba un número entero y retorne -1 si el número es negativo. Si el número es positivo debe devolver una clave calculada de la siguiente manera: Se suma cada dígito que compone el número y a esa suma se le calcula el modulo 7.
//Por ejemplo: para la cifra 513, la clave será +1+3 =9; 9 mod 7 = 2.
//Utilice la función para diseñar un algoritmo que permita leer varios valores y determine, para cada uno, si el número leído fue negativo o, si fue positivo, que clave le corresponde.

ACCION 124 ES

AMBIENTE

num:entero

FUNCION retorno (num:entero)entero ES
resp:entero
suma:entero
resp:=0
suma:=0
Si num<0 entonces
	resp:=-1
sino	
	Mientras num>0	
		suma:= suma + num MOD 10
		num:= num DIV 10
	finmientras
	resp:=suma MOD 7 
finsi
retorno:=resp
FINSUB

PROCESO

escribir("Ingresar un numero (o 0 para terminar)")
leer(num)
Mientras num<>0
	escribir("el numero ingresado es"retorno(num))
	escribir("Ingresar un numero (0 para terminar)")
	leer(num)
finmientras
FINACCION